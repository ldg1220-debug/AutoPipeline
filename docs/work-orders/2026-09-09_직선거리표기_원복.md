# 작업 지시서 — 직선거리 임시 표기 원복 (병합 전에)

작성: 2026-09-09 · 대상: **AutoPipeline** (`ldg1220-debug/AutoPipeline`)
근거: 트레쥴 `main` @ c87d759 (#242) 프로덕션 실측

---

## 0. 먼저 — 이 지시서를 저장소에 보관

1. `docs/work-orders/2026-09-09_직선거리표기_원복.md` 로 저장
2. **완료된** 지시서 중 오래된 것부터 지워 10개 유지
3. 본 작업과 **같은 PR에** 포함

---

## 1. ★★★ 임시 조치가 하루 만에 불필요해졌습니다

어제 `0fc360b`로 "직선거리 기준" 표기를 넣으셨는데,
**같은 날 밤 트레쥴이 실제 경로 조회를 붙였습니다.**

```
c87d759 | 이동 거리·시간을 직선거리 대신 실제 경로로 조회 (#242) | 09-08 23:22
```

프로덕션에서 확인했습니다.

| 지역 | API `totalDistanceKm` | 직선 합 | 비율 |
|---|---|---|---|
| 통영 d1 | **37.6km** | 23.47km | **1.60배** |
| 경주 d1 | **5.2km** | 3.77km | **1.38배** |

구간 속도도 더 이상 고정값이 아닙니다.

```
이전:  walk 4.8 / 4.8      car 24.8 / 24.0 / 25.6      ← 고정 속도
지금:  walk 4.8 / 5.0 / 5.1 / 5.2   car 24.3 / 12.5 / 8.2   ← 실제 경로
```

지도 경로선도 **도로를 따라갑니다.** 직각으로 꺾이며 골목을 지납니다.

**즉, `0fc360b`를 그대로 병합하면 정확해진 데이터에 "직선거리 기준"이라는
틀린 설명이 붙습니다.**

---

## 2. 수정 — 스위치만 끄면 됩니다

지시서대로 한 곳에 모아두신 덕분에 간단합니다.

```
monetizer.js:  STRAIGHT_LINE_DISTANCE_MODE = false
```

되돌아와야 할 것:

| 항목 | 상태 |
|---|---|
| TL;DR 소요시간 | 복원 |
| `이동수단: … 기준` 줄 | 복원 |
| 동선 타임라인 표 `다음까지` 열 | 복원 |
| `※ 거리는 … 직선거리 기준입니다` 고지 | 제거 |

**타임라인 표의 `다음까지` 열까지 함께 뺀 판단은 맞았습니다.**
같은 값을 쓰는 열이라 하나만 고치면 앞뒤가 안 맞았을 겁니다. 이제 같이 돌아오면 됩니다.

`STRAIGHT_LINE_DISTANCE_MODE` 스위치 자체는 **지우지 말고 남겨두세요.**
같은 상황이 또 생기면 한 줄로 대응됩니다.

---

## 3. ★★ PR #11이 나흘째 draft입니다

```
#11  여행 상식 게이트 + 동선 타임라인 표
     draft · 커밋 3개 · 파일 10개 · 생성 09-06

  3f135c5  여행 상식 게이트 + 동선 타임라인 표      09-06
  c8a8650  코스 지도 이미지 + 어투 잔여물 수정      09-08
  0fc360b  직선거리 임시 표기                      09-08
```

**세 건의 작업이 전부 여기 묶인 채 main에 못 올라가고 있습니다.**
그래서 코스 지도도, 어투 수정도 아직 발행 글에 반영되지 않았습니다.

제목("여행 상식 게이트 + 동선 타임라인 표")이 담긴 내용과도 맞지 않습니다.

```
제목을 "지시서 3건 반영 — 여행 상식 게이트·코스 지도·어투 수정" 같은 것으로 바꾸고
§2 원복 커밋을 얹은 뒤, ready-for-review → 병합
```

---

## 4. 순서

| | 작업 | 규모 |
|---|---|---|
| 1 | `STRAIGHT_LINE_DISTANCE_MODE = false` (§2) | 소 |
| 2 | PR #11 제목 정리 후 병합 (§3) | — |
| 3 | 해외 키워드 1편 발행 | — |

---

## 5. 발행 후 Cowork이 확인할 것

| 항목 | 기대 |
|---|---|
| TL;DR | 소요시간 있음, "직선거리 기준" 없음 |
| 이동수단 줄 | 있음 |
| 타임라인 표 | `다음까지` 열 있음 |
| 본문 첫 이미지 | 코스 지도 (`blob/course-maps`) |
| `30대 직장인` | 0건 |
| eSIM 링크 | 3개 |
